class $1 : RefCounted
{
    GDCLASS($1, RefCounted)

protected:

    static void _bind_methods()
    {
        # Bind getters and seters to godot
$2

    }

public:

    # Declare fields (in c++ types)
$3

    # Declare getters and setters (accept and return Godot types)
$4

};